Todo:
- OpenAL
    - 傅里叶变换频谱分析

- Debug
    - 为部分方法的失败返回，增加以周围"注释"为内容的日志输出。


Comments:
- 可恶的 OpenAL，它的 buffer 居然不属于 device ，而属于 context 。


- 多窗口机制实现建议：

    class Engine {
        stages;
    };

    class Stage {
        windows;
    };

    class Window {
        renderer;
    };

Some Words:

基于 Actor 的组件，要实现逻辑层交互，需另外计算变换后的渲染坐标，并以该坐标为交互所用的逻辑坐标。
